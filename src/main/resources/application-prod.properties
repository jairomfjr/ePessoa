# Production Profile Configuration
spring.application.name=ePessoa-Production
server.port=${PORT:8080}

# PostgreSQL Production Database
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/epessoa}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# JWT Configuration (usar variáveis de ambiente)
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# OAuth2 Gov.br Configuration (Production)
spring.security.oauth2.client.registration.govbr.client-id=${GOVBR_CLIENT_ID}
spring.security.oauth2.client.registration.govbr.client-secret=${GOVBR_CLIENT_SECRET}
spring.security.oauth2.client.provider.govbr.authorization-uri=https://sso.acesso.gov.br/authorize
spring.security.oauth2.client.provider.govbr.token-uri=https://sso.acesso.gov.br/token
spring.security.oauth2.client.provider.govbr.user-info-uri=https://sso.acesso.gov.br/userinfo

# Swagger (desabilitar em produção se necessário)
springdoc.swagger-ui.enabled=${SWAGGER_ENABLED:false}

# Logging (menos verboso em produção)
logging.level.root=WARN
logging.level.br.gov.epessoa=INFO
logging.level.org.springframework.security=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.file.name=logs/epessoa.log
logging.logback.rollingpolicy.max-file-size=10MB
logging.logback.rollingpolicy.max-history=30

# Actuator (limitar endpoints em produção)
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# Security
server.error.include-message=never
server.error.include-stacktrace=never
server.error.include-exception=false

# Compression
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,application/javascript,application/json

# SSL (descomentar se usar SSL direto no Spring Boot)
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=${SSL_KEY_STORE_PASSWORD}
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=epessoa

